import java.util.Scanner;

public class ConversionFun {
    public static void main(String[] args) {
        final Scanner input = new Scanner(System.in);
        // final long stringToLong = Long.parseLong(input.nextLine(), 16);
        // System.out.println(stringToLong);

        // final double stringToDouble = Double.parseDouble(input.nextLine());
        // System.out.println(stringToDouble);

        // final String doubleToString = String.valueOf(input.nextDouble());
        // System.out.println(doubleToString);

        System.out.println("Welcome to java".lastIndexOf("o", 15));
    }
}
