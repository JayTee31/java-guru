import java.util.Locale;

public class StringFest {
    public static void main(String[] args) {
        /* final String s1 = " Welcome ";
        final String s2 = " welcome ";

        // a, feladat
        final boolean isEqual = s1.equals(s2);

        // b, feladat
        isEqual = s1.equalsIgnoreCase(s2);

        // c, feladat
        final int x = s1.compareTo(s2);

        // d, feladat
        final int x = s1.compareToIgnoreCase(s2);

        // e, feladat
        final boolean b = s1.startsWith("aaa");

        // f, feladat
        b = s1.endsWith("aaa");

        // g feladat
        x = s1.length();

        // h feladat
        final char y = s1.charAt(0);

        // i feladat
        final String s3 = s1.concat(s2);

        // j, feladat
        s3 = s1.substring(1);

        // k, feladat
        s3 = s1.substring(1, 4);

        // l feladat
        s3 = s1.toLowerCase();

        // m feladat
        s3 = s1.toUpperCase();

        // n feladat
        s3 = s1.trim();

        // o feladat
        x = s1.indexOf('e');

        // p feladat
        x = s1.lastIndexOf("abc"); */

    }
}
