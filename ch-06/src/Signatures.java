public class Signatures {
    public static void main(String[] args) {
    }

    // return sales commission, commission rate, sales amount
    public static double calculateSalesCommission(double amount, double rate) {
        throw new IllegalStateException();
    }

    // display the calendar for a month given the month and the year
    public static void createCalendar(int year, int month) {
        throw new IllegalStateException();
    }

    // return a square root for a number
    public static double squareRoot(double number) {
        throw new IllegalStateException();
    }

    // test whether a number is even and returning true if it is
    public static boolean isEven(int number) {
        throw new IllegalStateException();
    }

    // Display a message a specified number of times
    public static void repeatMessage(int numberOfTimes, String message) {
        throw new IllegalStateException();
    }

    // return the monthly payment given the loan amount number of years and annual interest rate
    public static double calculateMonthlyPayment(double loanAmount, double numberOfYears, double annualInterestRate) {
        throw new IllegalStateException();
    }

    // return the corresponding uppercase letter given lowercase letter
    public static char toLowercase(char uppercaseLetter) {
        throw new IllegalStateException();
    }
}
